# Backend
DATABASE_URL=postgresql://asset:asset@localhost:5432/asset
JWT_SECRET=change-me
AWS_S3_BUCKET=asset-files
PORT=3001

# SMTP — fill when IT shares credentials
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
MAIL_FROM=
MAIL_FROM_NAME=Asset Management

EMPLOYEE_LOGIN_PASSWORD=Asset@123

# Frontend (put VITE_* in frontend/.env — Vite reads that folder)
VITE_API_URL=http://localhost:3001
# default | mhs | nord | catppuccin | tokyo | github | linear
VITE_SKIN=default
