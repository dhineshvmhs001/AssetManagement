PORT=5000
JWT_SECRET=change-me
DATABASE_URL=postgresql://asset:asset@localhost:5432/asset

# SMTP — fill when IT shares credentials
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
MAIL_FROM=
MAIL_FROM_NAME=Asset Management

# Temporary password for new employee logins (role EMPLOYEE)
EMPLOYEE_LOGIN_PASSWORD=Asset@123

# Public backend URL used in manager approve/reject email links
APP_URL=http://localhost:5000
